const { createClient } = require('@insforge/sdk'); const supabase = createClient({baseUrl: 'http', anonKey: 'key'}); console.log('signIn:', supabase.auth.signInWithPassword.toString().substring(0, 100)); console.log('signUp:', supabase.auth.signUp.toString().substring(0, 100)); console.log('verifyEmail:', supabase.auth.verifyEmail.toString().substring(0, 100)); console.log('getCurrentSession:', supabase.auth.getCurrentSession.toString().substring(0, 100));
